<%= @doctor.name %>
<br>
<%= @doctor.appointments.each do |app| %>
    <% @pat = @doctor.patients.find_by_id(app.patient_id) %>
    <%= link_to @pat.name, patient_path(@pat) %>
    <%= link_to app.appointment_datetime.strftime("%B %d, %Y at %k:%M"), appointment_path(app) %>
<% end %>